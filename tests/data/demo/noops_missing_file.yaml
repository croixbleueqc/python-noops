# pipeline.image.ci is set with a missing file
# in the product and in the devops directory

metadata:
  version: 1

devops:
  local:
    path: devops

pipeline:
  image:
    ci: scripts/missing-image-ci.sh
